<h1>List</h1>

<ul>
<% @events.each do |event| %>
  <li>
 	  <%= event.name %>
  <%= link_to 'Show', :controller => 'club', :action => 'show', :id => event %>
  <%= link_to 'Edit', :controller => 'club', :action => 'edit', :id => event %>
  <%= link_to 'Delete', :controller => 'club', :action => 'destroy', :id => event %>
  </li>
<% end %>
</ul>
<%= link_to 'New Event', :controller => 'club', :action => 'new' %>